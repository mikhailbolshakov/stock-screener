spring:
  application:
    name: stock-screener
  cloud:
    consul:
      host: localhost
      port: 8500
      enabled: false
      config:
        enabled: false
        format: FILES
    bus:
      enabled: false

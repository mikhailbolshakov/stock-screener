spring:
  application:
    name: stock-screener
  cloud:
    consul:
      host: localhost
      port: 8500
      enabled: true
      config:
        enabled: true
        format: FILES
    bus:
      enabled: false
